@model string[]
@{
}
 
<td style="width: 150px">
    <div class="btn-group">
        <a asp-action="Edit" asp-controller="@Model[0]" class="btn btn-primary" asp-route-id="@Model[1]">Edit</a>
        <a asp-action="Details" asp-controller="@Model[0]" class="btn btn-success" asp-route-id="@Model[1]">Details</a>
        <a asp-action="Delete" asp-controller="@Model[0]" class="btn btn-danger " asp-route-id="@Model[1]">Delete</a>
    </div>
</td>